# frozen_string_literal: true

RSpec.describe Aws::Spec::Generator do
  it 'has a version number' do
    expect(Aws::Spec::Generator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
